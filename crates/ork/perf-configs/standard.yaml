# Standard load test - typical workload
# Medium load to measure normal operating characteristics
workflows: 100
tasks_per_workflow: 5
duration: 0.1

scheduler:
  poll_interval_secs: 2
  max_tasks_per_batch: 100
  max_concurrent_dispatches: 50
  max_concurrent_status_checks: 100
