[project]
name = "ork-deferrables-example"
version = "0.1.0"
description = "Example workflow demonstrating deferrables with Ork"
requires-python = ">=3.8"
dependencies = [
    "ork-sdk",
]

[tool.uv]
package = false

[tool.uv.sources]
ork-sdk = { path = "../../../python-sdk" }
