[project]
name = "ork-deferrables-example"
version = "0.1.0"
description = "Example workflow demonstrating deferrables with Ork"
requires-python = ">=3.8"
dependencies = [
    "ork-sdk @ file:///${PROJECT_ROOT}/../../../python-sdk",
]

[tool.uv]
package = false
